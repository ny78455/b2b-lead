# ── Core ─────────────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1
pydantic-settings==2.2.1
python-multipart==0.0.9
python-dotenv==1.0.1

# ── Database ──────────────────────────────────────────────────────────────────
sqlalchemy[asyncio]==2.0.30
asyncpg==0.29.0
alembic==1.13.1

# ── HTTP / Scraping ───────────────────────────────────────────────────────────
httpx==0.27.0
beautifulsoup4==4.12.3
playwright==1.44.0
urllib3==2.2.1

# ── LLM — Gemma 4 2B local (PRIMARY) ─────────────────────────────────────────
# transformers + a PyTorch backend are required to load the model locally.
# Install PyTorch separately following https://pytorch.org/get-started/locally/
# (choose the CUDA build if you have a GPU, otherwise the CPU build).
transformers>=4.47.0
accelerate>=0.30.0          # required for device_map="auto"
sentencepiece>=0.2.0        # Gemma tokeniser dependency

# ── LLM — Gemini API (FALLBACK, optional) ────────────────────────────────────
# Only needed if you want the Gemini API fallback to work.
# Leave GEMINI_API_KEY="" in .env to disable.
google-generativeai==0.7.2

# ── Google Sheets ─────────────────────────────────────────────────────────────
gspread==6.1.2
google-auth==2.29.0

# ── IMAP / Reply detection ────────────────────────────────────────────────────
aioimaplib==1.1.0

# ── Testing ───────────────────────────────────────────────────────────────────
pytest==8.2.0
pytest-asyncio==0.23.7
httpx==0.27.0    # already listed, used as test client too
